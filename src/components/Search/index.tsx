import * as Styled from './styles';

const Search = () => {
  return (
    <Styled.Container>
      <input
        type="text"
        placeholder="buscar"
      />
    </Styled.Container>
  );
};

export default Search;